namespace ConsoleTaskManager.Models
{
    public enum UserRole
    {
        Manager,
        Employee
    }

    public enum ProjectTaskStatus
    {
        ToDo,
        InProgress,
        Done
    }
}